@model Tienda.Models.proveedor

@{
    ViewBag.Title = "Detalles";
}

<h2>Detalles</h2>

<div>
    <h4>Proveedor</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.NOMBRE_PROVEEDOR)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.NOMBRE_PROVEEDOR)
        </dd>

    </dl>
</div>
<p>
    @Html.ActionLink("Editar", "Edit", new { id = Model.ID_PROVEEDOR }) |
    @Html.ActionLink("Regresar a Lista", "Index")
</p>
