@model IEnumerable<Tienda.Models.sexo>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SEXO1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SEXO1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_SEXO }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_SEXO }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_SEXO })
        </td>
    </tr>
}

</table>
