@model Tienda.Models.sexo

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>sexo</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.SEXO1)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.SEXO1)
        </dd>

    </dl>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.ID_SEXO }) |
    @Html.ActionLink("Back to List", "Index")
</p>
